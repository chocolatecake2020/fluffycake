# Payment Logo Assets

Replace these files with official brand-approved SVG assets before production:

- `stripe.svg`
- `paypal.svg`
- `usdt.svg`

The UI component uses these exact filenames in `src/components/payments/PaymentLogoStrip.jsx`.
